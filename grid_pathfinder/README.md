# How to run 
Go to the main directory where the code is i.e grid_pathfinder.
```
cd grid_pathfinder
```
To run it we can run python command:
```
python3 -m http.server 8000
```

Don't forget to install requirements if missing :)
